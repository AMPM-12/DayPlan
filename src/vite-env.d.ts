/// <reference types="vite/client" />

/** Injected by vite.config.ts's `define` — short commit hash of the build. */
declare const __BUILD_COMMIT__: string
/** Injected by vite.config.ts's `define` — ISO timestamp of when `vite build` ran. */
declare const __BUILD_TIME__: string
